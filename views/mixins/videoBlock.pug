mixin videoBlock(video={})
        .videoBlock
            video.videoBlock__thumbNail(src=video.file, controls=true)
            a(href=routes.videoDetail(video.id))
                h3.videoBlock__title=video.title
                h4.videoBlock__views=video.views
