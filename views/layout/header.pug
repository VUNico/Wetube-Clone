header.header
    .header__column
        h1 #{siteName}
    .header__column
        ul
            li
                a(href=routes.join) Join
            li
                a(href=routes.login) Log In